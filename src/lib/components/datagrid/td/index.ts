export { default as Td } from './datagrid-td.svelte';
export * from './bond.svelte';

// ── Semantic alias ────────────────────────────────────────────────────────
/** Preferred alias for Td — use Cell going forward */
export { default as Cell } from './datagrid-td.svelte';
