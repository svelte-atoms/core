export { default as Tr } from './datagrid-row.svelte';
export * from './bond.svelte';

// ── Semantic alias ────────────────────────────────────────────────────────
/** Preferred alias for Tr — use Row going forward */
export { default as Row } from './datagrid-row.svelte';
