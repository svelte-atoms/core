import { Meta } from '@storybook/addon-docs/blocks';

import Github from './assets/github.svg';
import Tutorials from './assets/tutorials.svg';
import Styling from './assets/styling.png';
import Context from './assets/context.png';
import Assets from './assets/assets.png';
import Docs from './assets/docs.png';
import Testing from './assets/testing.png';
import Accessibility from './assets/accessibility.png';
import Theming from './assets/theming.png';

export const RightArrow = () => (
	<svg
		viewBox="0 0 14 14"
		width="8px"
		height="14px"
		style={{
			marginLeft: '4px',
			display: 'inline-block',
			shapeRendering: 'inherit',
			verticalAlign: 'middle',
			fill: 'currentColor',
			'path fill': 'currentColor'
		}}
	>
		<path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
	</svg>
);

<Meta title="Welcome" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Svelte Atoms

    **`@ixirjs/ui`** is a modern, headless, and accessible Svelte 5 UI
    component library built for composability and extensibility. Components ship
    unstyled with sensible defaults, expose their full reactive state, and can be
    re-shaped, re-rooted, and re-styled without forking.

    This Storybook is the living reference for every component. Browse the sidebar
    to see each one in all its variations, read its props, and copy working code.

  </div>
  <div className="sb-section">
    <div className="sb-section-item">
      <img
        src={Styling}
        alt="An abstraction representing headless, unstyled components"
      />
      <h4 className="sb-section-item-heading">Headless first</h4>
      <p className="sb-section-item-paragraph">Components own behavior, accessibility, and state — not your visual design. Style every part with your own classes, or lean on the built-in preset system.</p>
      <a
        href="https://github.com/ixirjs/ui#readme"
        target="_blank"
      >Learn more<RightArrow /></a>
    </div>
    <div className="sb-section-item">
      <img
        src={Context}
        alt="An abstraction representing composition of bonds and atoms"
      />
      <h4 className="sb-section-item-heading">Bond architecture</h4>
      <p className="sb-section-item-paragraph">Each component has one Bond that owns its reactive state and mints atoms (its slots). State mutates through methods like <code>open()</code>, <code>toggle()</code>, and <code>select()</code> — never ad-hoc prop writes.</p>
      <a
        href="https://deepwiki.com/ixirjs/ui"
        target="_blank"
      >Learn more<RightArrow /></a>
    </div>
    <div className="sb-section-item">
      <img src={Assets} alt="A representation of composable building blocks" />
      <div>
        <h4 className="sb-section-item-heading">Composable by design</h4>
        <p className="sb-section-item-paragraph">Re-root any component with <code>base</code> / <code>as</code>, reach the bond through typed snippets, and extend behavior with reusable capabilities — compose complex UI from small, predictable atoms.</p>
        <a
          href="https://github.com/ixirjs/ui#readme"
          target="_blank"
        >Learn more<RightArrow /></a>
      </div>
    </div>
  </div>
</div>
<div className="sb-container">
  <div className='sb-section-title'>
    # Get started

    Install the package and import only what you use — every component is a
    dedicated entry point, so your bundle stays lean.

  </div>

```bash
npm install @ixirjs/ui
```

```svelte
<script lang="ts">
	import { Button, Dialog } from '@ixirjs/ui';

	let open = $state(false);
</script>

<Button onclick={() => (open = true)}>Open dialog</Button>

<Dialog.Root bind:open>
	<Dialog.Content>
		<Dialog.Header>
			<Dialog.Title>Welcome</Dialog.Title>
		</Dialog.Header>
		<Dialog.Body>A headless, accessible dialog — styled entirely by you.</Dialog.Body>
	</Dialog.Content>
</Dialog.Root>
```

</div>
<div className="sb-container">
  <div className='sb-section-title'>
    # What you get

    Built on Svelte 5 runes and snippets, with the cross-cutting concerns of a
    real component library handled for you.

  </div>

  <div className="sb-section">
    <div className="sb-features-grid">
      <div className="sb-grid-item">
        <img src={Accessibility} alt="A representation of accessible, keyboard-navigable components" />
        <h4 className="sb-section-item-heading">Accessible</h4>
        <p className="sb-section-item-paragraph">ARIA attributes, keyboard
          navigation, and focus management are wired into every component out of the box.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Theming} alt="A representation of presets and theming" />
        <h4 className="sb-section-item-heading">Presets &amp; variants</h4>
        <p className="sb-section-item-paragraph">A dotted preset hierarchy and a
          Tailwind-aware merge kernel let you theme every atom without prop drilling.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Testing} alt="A representation of type-safe component APIs" />
        <h4 className="sb-section-item-heading">Type safe</h4>
        <p className="sb-section-item-paragraph">Full TypeScript inference across
          bonds, atoms, snippets, and props — with types you can extend, not fight.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Docs} alt="A representation of an extensible capability seam" />
        <h4 className="sb-section-item-heading">Extensible</h4>
        <p className="sb-section-item-paragraph">Add behavior with reusable
          capabilities and the <code>use()</code> seam — extend components without subclassing or forking.</p>
      </div>
    </div>
  </div>
</div>

<div className="sb-section sb-socials">
    <div className="sb-section-item">
      <img src={Github} alt="GitHub logo" className="sb-explore-image"/>
      Read the source, file issues, and follow the roadmap.

      <a
        href="https://github.com/ixirjs/ui"
        target="_blank"
      >View on GitHub<RightArrow /></a>
    </div>
    <div className="sb-section-item">
      <img src={Tutorials} alt="A book representing documentation" className="sb-explore-image"/>
      <div>
        Explore architecture and concepts in depth.

        <a
          href="https://deepwiki.com/ixirjs/ui"
          target="_blank"
        >Browse the docs<RightArrow /></a>
      </div>
    </div>
    <div className="sb-section-item">
      <img src={Assets} alt="The npm package" className="sb-explore-image"/>
      <div>
        Install the latest release from the registry.

        <a
          href="https://www.npmjs.com/package/@ixirjs/ui"
          target="_blank"
        >View on npm<RightArrow /></a>
      </div>
    </div>

</div>

<style>
	{`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }

  img {
    object-fit: cover;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section a:not(h1 a, h2 a, h3 a) {
    font-size: 14px;
  }

  .sb-section-item, .sb-grid-item {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .sb-section-item-heading {
    padding-top: 20px !important;
    padding-bottom: 5px !important;
    margin: 0 !important;
  }
  .sb-section-item-paragraph {
    margin: 0;
    padding-bottom: 10px;
  }

  .sb-chevron {
    margin-left: 5px;
  }

  .sb-features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-gap: 32px 20px;
  }

  .sb-socials {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
  }

  .sb-socials p {
    margin-bottom: 10px;
  }

  .sb-explore-image {
    max-height: 32px;
    align-self: flex-start;
  }

  @media screen and (max-width: 600px) {
    .sb-section {
      flex-direction: column;
    }

    .sb-features-grid {
      grid-template-columns: repeat(1, 1fr);
    }

    .sb-socials {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  `}
</style>
